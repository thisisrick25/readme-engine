{
  "name": "github-action",
  "version": "2.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build src/index.ts --license licenses.txt",
    "prebuild": "npm run generate-plugins",
    "test:local": "node --import tsx tests/local-test.ts",
    "pretest:local": "npm run generate-plugins",
    "test:integration": "act --job run-local-test --workflows tests/local-test-workflow.yml",
    "generate-plugins": "node scripts/generate-plugin-registry.js",
    "clean": "rimraf ./dist",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@vercel/ncc": "^0.38.3",
    "dotenv": "^17.2.1",
    "husky": "^9.1.7",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
